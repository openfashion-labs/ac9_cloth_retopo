"""Mesh Edit sub-property group (AC9ClothRetopoProps.mesh_edit).

Currently empty: the result line lives on the top-level props (status_faces).
Kept so the sub-package keeps the same shape as the others.
"""

from bpy.types import PropertyGroup


class AC9MeshEditProps(PropertyGroup):
    pass
