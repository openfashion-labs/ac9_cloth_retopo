# コントリビューションガイド

このプロジェクトへの貢献に興味を持っていただきありがとうございます！

English version: [CONTRIBUTING.md](CONTRIBUTING.md)

## 貢献のしかた

### Issue の報告

- 重複を避けるため、まず[既存の Issue](../../issues) を検索してください。
- バグ報告には、再現手順・期待する動作と実際の動作・環境（OS、Blender のバージョンなど）を含めてください。
- 機能要望や質問も Issue として歓迎します。

### プルリクエスト（PR）

1. リポジトリを fork する（外部コントリビューター）か、`main` からブランチを切る（チームメンバー）。
2. 自分のブランチ上で変更を行う。
3. 「何を・なぜ」変更したかを明記して `main` に向けた PR を作成する。
4. コードオーナーがレビューします。レビューコメントには対応をお願いします。
5. マージは squash merge で行われるため、ブランチ上の作業途中のコミットはそのままで構いません。

`main` への直接プッシュは無効化されており、すべての変更は PR を経由します。

## ルール

### 秘密情報・プライベートな情報の禁止

以下は絶対にコミットしないでください:

- **認証情報** — API キーやトークン（`sk-...` や `ghp_...` のような文字列）、
  パスワード、秘密鍵、`.env` ファイル。
- **個人情報** — 実在の人物の氏名、メールアドレス、ボディスキャンや採寸データ、
  その他の個人を特定できる情報。
- **社内・顧客の資料** — 社内限定のドキュメント、顧客データ、
  社内のファイルパスや内部文書名、その他公開を意図していないもの。

これはファイルの中身だけでなく、**コミットメッセージや PR の説明文にも適用されます**
（リポジトリと一緒に公開されるため）。

すべての PR は CI で [gitleaks](https://github.com/gitleaks/gitleaks) による
スキャンが実行され、スキャンが失敗している間はマージがブロックされます。
ただし gitleaks が検出できるのは認証情報だけで、**個人情報や社内資料は
自動検出されません**。コミット前にファイルを必ず見直してください。
万一秘密情報をコミットしてしまったら、直ちに失効（無効化）させてください —
一度プッシュされた後は、ブランチから削除するだけでは不十分です。

### 大容量ファイル

バイナリアセットは小さく保ってください。50 MB 超のファイルは警告が出て、
100 MB 超は GitHub に拒否されます。大きな 3D アセットが必要な貢献をしたい
場合は、扱い方を相談するため先に Issue を立ててください。

## チームメンバー向け

社内のコントリビューターは、社内の作業フローとルールをまとめた
[チームガイド](docs/team-guide.ja.md)を参照してください。

## ライセンス

貢献を行うことにより、あなたの貢献がこのプロジェクトと同じライセンス
（[LICENSE](LICENSE) を参照）で提供されることに同意したものとみなされます。
